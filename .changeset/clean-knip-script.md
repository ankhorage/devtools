---
'@ankhorage/devtools': patch
---

Rename the synchronized Knip package script to `knip:check` so Expo Doctor no longer rejects generated Expo applications for colliding with the installed `knip` binary.
