---
'@ankhorage/devtools': patch
---

Expose the canonical Bun runtime policy through the public `@ankhorage/devtools/policy` package subpath so validation packages can consume it without duplicating the policy version.
